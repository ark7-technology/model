import { Enum } from '../../src';

export const genderMetadata: any = {
  superClass: Enum.prototype.constructor,
  configs: {
    schema: {
      name: 'Gender',
      props: [],
    },
  },
  fields: {},
  name: 'Gender',
};
